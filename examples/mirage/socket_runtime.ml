(* Runtime configuration for the Unix website socket stack.

   Values injected as labeled arguments into connect calls
   via the Socket.Runtime component convention. *)

let ipv4_only = false
let ipv6_only = false
